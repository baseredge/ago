# 统一换行符处理，避免 Windows/跨平台协作时的 LF/CRLF 混乱
# 仓库内统一存储为 LF，checkout 时按操作系统转换

* text=auto eol=lf

# Go 源码强制 LF
*.go text eol=lf

# 文档/配置统一 LF
*.md text eol=lf
*.json text eol=lf
*.mod text eol=lf
*.sum text eol=lf

# Windows 批处理保持 CRLF
*.bat text eol=crlf
*.ps1 text eol=crlf

# 二进制文件不转换
*.png binary
*.jpg binary
*.ico binary
*.exe binary
